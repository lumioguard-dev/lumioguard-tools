{
  "name": "@lumioguard/citecheck-api",
  "version": "0.13.0",
  "private": true,
  "type": "module",
  "description": "Citecheck scan API: the page and well-known fetches, the crawler-agent comparison and the clock. Cloudflare Worker.",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "node ../../../scripts/dev-worker.mjs citecheck",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "node ../../../scripts/deploy-worker.mjs citecheck",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@lumioguard/api-core": "workspace:*",
    "@lumioguard/citecheck-core": "workspace:*",
    "@lumioguard/shared": "workspace:*",
    "hono": "~4.13.5",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260815.1",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.42.0"
  }
}
