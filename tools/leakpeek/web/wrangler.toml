# Cloudflare Pages. The Pages project is created out of band; this file encodes
# the static build target. `/api/*` is served by the leakpeek-api Worker, wired
# through the Pages routes UI or a _routes.json.
name = "lumioguard-leakpeek-web"
compatibility_date = "2024-12-18"
pages_build_output_dir = "dist"
